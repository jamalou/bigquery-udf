function_type: function_sql
project: manifest-canto-413823
dataset: udf
description: Return square root of an int
arguments:
  - name: x
    type: int64
output:
  name: square
  type: int64
examples:
  - description: ""
    arguments:
      - "5"
    output: "25"
code: |
  {{ ref("plus", env=env) }}(x) * {{ ref("minus", env=env) }}(x)
tests:
  - description: simple_test
    file: square.json
